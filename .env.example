# Copy per-app examples instead; this file only documents the full set.
# Backend: apps/backend/.env  — see apps/backend/.env.example
# Dashboard: apps/dashboard/.env.local — see apps/dashboard/.env.example (Task 13)
# Classification defaults to regex-only; local Ollama inference (see
# apps/backend/.env.example) is implemented and selectable but not the shipped
# default. No third-party AI API key is required or used either way.
