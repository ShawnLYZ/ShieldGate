{
  "name": "extension",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wxt",
    "build": "wxt build",
    "test": "vitest run",
    "postinstall": "wxt prepare"
  },
  "dependencies": { "@shieldgate/policy": "workspace:*" },
  "devDependencies": {
    "@types/chrome": "^0.2.2", "typescript": "^5.8.3", "vitest": "^3.1.0", "wxt": "^0.20.0"
  }
}
